// import emails from './source/quickgel.js'
// import validate from './validateEmails'

const testing = () => {
	// Validating emails
	//const result = validate(emails)
	// function myTrim(x) {
	// 	return x.replace(/^\s+|\s+$/gm, '')
	// }
	//
	// const res = emails.split(',').map(email => {
	// 	email = myTrim(email)
	// 	email = email.toLowerCase()
	//
	// 	const emailsReduced = emails.replace(email, '')
	// 	if (emailsReduced.includes(email)) {
	// 		console.log(email)
	// 	}
	// 	return email
	// })
	//console.log(result)
	// return
}

export default testing
